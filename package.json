{
  "name": "mirror-seo-pages",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "node build.js",
    "dev": "node build.js && npx serve dist"
  },
  "dependencies": {
    "front-matter": "^4.0.2",
    "marked": "^15.0.0"
  }
}
